# 2.4.6 - 2026-08-04

## Added

- `playToastTransition(id, transition)` method to play a transition animation on a toast without changing its content — useful for drawing attention back to a toast waiting on user input without patching it via `updateToast`.
