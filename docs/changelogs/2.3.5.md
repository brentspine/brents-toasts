# 2.3.5 - 2026-08-02

- Fixed: Multiple `Toasts` instances sharing the same snackbar position now correctly delegate state operations (update, data access, timer control, button/detail mutations) to the owning instance, preventing state corruption and ensuring reliable behavior when managing toasts across instances.
