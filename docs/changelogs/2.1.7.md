# 2.1.7 - 2026-07-23

## Changed

- Toast positioning now supports stacking away from both top and bottom edges, preparing the library for future position implementations beyond `BOTTOM_CENTER`. Toasts now dynamically use the `top` or `bottom` CSS property based on their position, and the positioning engine is edge-agnostic.
