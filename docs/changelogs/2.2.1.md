# 2.2.1 - 2026-07-24

## Added

- `reverseOrder` option to insert toasts at the far end of their position's stack instead of nearest the anchor edge, with builder method `andReverseOrder()`

## Changed

- Toast eviction now tracks creation order independently of DOM position to correctly identify the oldest toast when `reverseOrder` toasts are present
