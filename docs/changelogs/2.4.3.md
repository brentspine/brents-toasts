# 2.4.3 - 2026-08-03

## Added

- `toasts.promise(promise, messages, options?)` method for tying a toast to a Promise's lifecycle — shows a sticky loading toast that automatically patches to success or error once the promise settles, with support for outcome-dependent messages via callback functions.
