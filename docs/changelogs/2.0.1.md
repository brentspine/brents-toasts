# 2.0.1 - 2026-07-22

This is a large initial commit adding foundational tooling and documentation for the library. Since these are primarily CI/CD, config, documentation, and demo files (not API changes), the changelog entry should be minimal or empty.

**No user-facing changes.** This commit adds GitHub Actions workflows, build tooling, demo application, and documentation — all supporting infrastructure with no impact on the library's public API or functionality.

If a changelog entry is truly required for completeness:

- **Added** automated release pipeline with AI-generated changelogs and live demo deployment.

(But realistically, this should not appear in user-facing release notes — it's tooling overhead.)
