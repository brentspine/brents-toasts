# 2.2.6 - 2026-07-25

# Added

- `allowLineBreaks` option to control whether `\n` and `<br>`/`<br/>` render as line breaks in message, title, button labels, and details items. Defaults to `true` for backward compatibility; set to `false` to treat them as inert text.
