# 2.4.2 - 2026-08-02

## Added

- `ToastBuilder.withTitleMode()` method to set `titleMode` independently of `withTitle()`, allowing flexible control over title rendering mode when title is set elsewhere in the chain.
