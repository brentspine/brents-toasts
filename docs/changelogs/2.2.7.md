# 2.2.7 - 2026-07-25

## Changed

- Demo application rebuilt as a full Angular application instead of vanilla JavaScript, now served from `demo/dist/demo/browser/` with proper build tooling
- Release workflow upgraded to Node.js 22
- Demo deployment now uses content-hashed filenames; version archive structure remains stable while the latest build is completely replaced on each release
- Options snapshot (`options.json`) now captured and committed alongside changelog for each version
